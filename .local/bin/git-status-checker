#!/bin/sh

status() {
  # for d in $(ls -d ~/all-repos/*/); do
  for d in ~/all-repos/*/; do
    echo "$d"
    git -c color.ui=always -C "$d" status -s
  done

  echo "$HOME"
  dotbare -c color.ui=always -C "$HOME" status -s
}

status | less -FR
