#!/bin/sh

[ $(xrandr | grep -cw 'connected') == 1 ] && \
  xrandr --auto || \
  xrandr --auto --output $(xrandr | grep -w 'primary' | awk '{print $1}') --auto --right-of $(xrandr | grep -v 'primary' | grep -w 'connected' | awk '{print $1}')
